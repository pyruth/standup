{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "popup",
  "description": "The reminder popup can only render packaged local assets.",
  "windows": ["reminder-*"],
  "permissions": []
}
